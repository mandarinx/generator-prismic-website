{
  "name": "<%= projectname %>",
  "version": "0.0.1",
  "description": "<%= projectdesc %>",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nf -j Procfile_dev -p 5000 start"
  },
  "repository": {
    "type": "git",
    "url": "<%= gitrepo %>"
  },
  "author": "<%= author %>",
  "license": "<%= license %>",
  "dependencies": {
  },
  "devDependencies": {
  },
  "engines": {
    "node": "0.10.35"
  }
}
